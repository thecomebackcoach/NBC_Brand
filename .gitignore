.DS_Store
node_modules/
*.log
.env
.vscode/
.idea/
~$*

# Working files (regenerable from sources)
*.tmp
build/
dist/

# Claude Code — ignore local-only settings, but commit skills so they ship with the repo
.claude/settings.local.json
